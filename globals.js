// 🌐 VARIÁVEIS GLOBAIS
// Este arquivo deve ser carregado ANTES de todos os outros componentes

// Declarações do React (disponíveis globalmente para todos os arquivos)
const { useState, useEffect, createElement: h } = React;

// Declarações dos ícones (disponíveis globalmente)
const { MapPin, Calendar, Users, Plus, Bell, X, Search, Gift, Cake, StickyNote } = window.Icons;
